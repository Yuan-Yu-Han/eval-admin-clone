{
  "name": "eval-admin-clone",
  "version": "0.1.0",
  "private": true,
  "description": "One-to-one local demo for the /admin/eval page with mock APIs.",
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('static frontend: public/index.html + public/assets (no bundle step)')\"",
    "dev": "npm run dev:api",
    "dev:api": "node server.js",
    "start": "npm run start:api",
    "start:api": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "playwright-core": "^1.59.1"
  }
}
